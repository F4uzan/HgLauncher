<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <mono.hg.views.IndeterminateMaterialProgressBar
        android:id="@+id/load_progress"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:visibility="visible" />

    <com.simplecityapps.recyclerview_fastscroll.views.FastScrollRecyclerView
        android:id="@+id/apps_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/desc_apps_list"
        android:nextFocusDown="@id/search"
        android:nextFocusForward="@id/search"
        android:splitMotionEvents="false"
        android:clipToPadding="false"
        app:fastScrollPopupBgColor="@color/fastScrollerHold"
        app:fastScrollThumbColor="@color/fastScrollerHold"
        app:fastScrollThumbInactiveColor="@color/fastScrollerIdle" />

</FrameLayout>